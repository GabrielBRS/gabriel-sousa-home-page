name: Deploy Portfolio SSH
on: [push]

jobs:
  automation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Código
        uses: actions/checkout@v3

      - name: Deploy via SSH (Rsync)
        uses: Burnett01/rsync-deployments@5.2.1
        with:
          switches: -avzr --delete -O --no-perms --exclude='.git*' --exclude='.idea' --exclude='Dockerfile'
          path: personal-home-page/
          remote_path: /home/gabrielsousa/home-page-data/
          remote_host: 192.168.15.200
          remote_user: gabrielsousa
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}